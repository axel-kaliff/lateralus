# Create /home/linuxbrew directory on boot (before brew-setup.service installs brew)
# On ostree, /home -> /var/home is persistent and starts empty — tmpfiles.d ensures
# the directory exists with correct ownership on every boot
d /var/home/linuxbrew 0755 linuxbrew linuxbrew -
