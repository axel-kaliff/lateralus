# Create the linuxbrew system user for Homebrew
# sysusers.d runs before tmpfiles.d, ensuring the user exists when
# /var/home/linuxbrew is created. This also survives /etc/passwd 3-way
# merge issues on rebase (where the build-time user might be lost).
u linuxbrew - "Homebrew" /home/linuxbrew
